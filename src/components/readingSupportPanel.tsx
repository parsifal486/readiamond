const readingSupportPanel = ({selectedWord, selectedSentence}: {selectedWord: string, selectedSentence: string}) => {
  

  return (
    <div>{selectedWord} {selectedSentence}</div>
  )
}

export default readingSupportPanel